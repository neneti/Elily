- relationship = current_user.active_relationships.find_by(followed_id: user.id)
= form_with model: relationship, method: :delete, remote: true do |f|
  .mt-3
    = f.submit 'フォロー中', class: 'btn btn-sm unfollow-btn'
